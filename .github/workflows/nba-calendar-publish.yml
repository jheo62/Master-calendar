name: NBA Calendar Publish LEGACY DISABLED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy publication route disabled
        run: |
          echo "This legacy publication workflow is disabled."
          echo "Use NBA Calendar Publish Live or NBA Calendar Publish XLSX."
          exit 1
